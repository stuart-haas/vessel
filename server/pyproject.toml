[project]
name = "vessel-api"
version = "0.1.0"
description = "Vessel FastAPI backend — connects thoughts with biblical scripture."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0",
    "sqlmodel==0.0.22",
    "httpx==0.28.1",
    "pydantic-settings==2.7.1",
]

[dependency-groups]
dev = [
    "pytest==8.3.4",
]

[tool.uv]
package = false
